language: python
python:
  - "3.8"
install:
  - pip install -r requirements.txt
  - pip install codecov
script:
  - coverage python -m unittest discover -s ./src -p "Test*.py"
after_success:
  - codecov
